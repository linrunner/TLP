# tlp - udev rules

# handle change of power source ac/bat
ACTION=="change", SUBSYSTEM=="power_supply", ENV{POWER_SUPPLY_NAME}=="AC", RUN+="tlp auto"

# handle added usb devices (exclude subdevices)
ACTION=="add", SUBSYSTEM=="usb", DRIVER=="usb", ENV{DEVTYPE}=="usb_device", RUN+="tlp-usb-udev %p"
